import { TelNumFormatDirective } from './tel-num-format.directive';

describe('TelNumFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new TelNumFormatDirective();
    expect(directive).toBeTruthy();
  });
});
